def Selectionsort(List):
    for i in range(len(List)):
        Min=i
        for j in range(i+1,len(List)):
            if List[j]<List[i]:
             Min=j
        temp=List[Min]
        List[Min]=List[i]
        List[i]=temp
        print(List)

L=[9,6,8,3,4]
Selectionsort(L)
